<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Skunk Report</title>
  <style>
    /* Modern CSS Reset */
    *, *::before, *::after {
      box-sizing: border-box;
    }

    * {
      margin: 0;
      padding: 0;
    }

    html, body {
      height: 100%;
    }

    body {
      line-height: 1.5;
      -webkit-font-smoothing: antialiased;
    }

    img, picture, video, canvas, svg {
      display: block;
      max-width: 100%;
    }

    p, h1, h2, h3, h4, h5, h6 {
      overflow-wrap: break-word;
    }
    /* End Modern CSS Reset */

    :root {
      /* Color Palette */
      --primary-gradient-start:rgb(8, 58, 19);
      --primary-gradient-end: rgb(8, 98, 44);;
      --background-color: #f5f5f5;
      --text-color: #333;
      --text-color-light: #666;
      --white: #ffffff;
      --border-color: #eee;
      --shadow-color: rgba(0,0,0,0.1);

      --cards-background-color: #f8f9fa;
      --card-background-color: #ffffff;

      /* Skunk Score Colors */
      --score-high: #e74c3c;
      --score-medium: #f39c12;
      --score-low: #27ae60;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 0;
      padding: 2rem;
      background-color: var(--background-color);
      color: var(--text-color);
    }
    .container {
      margin: 0 auto;
      background: var(--white);
      border-radius: 2rem;
      box-shadow: 0 2px 10px var(--shadow-color);
      overflow: hidden;
    }
    .header {
      background: linear-gradient(135deg, var(--primary-gradient-start) 0%, var(--primary-gradient-end) 100%);
      color: var(--white);
      padding: 2rem;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .header h1 {
      font-size: 3rem;
      font-weight: 300;
    }
    .header p {
      font-size: 1.5rem;
    }
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 2rem;
      padding: 2rem;
      background: var(--cards-background-color);
    }

    .card {
      background: var(--card-background-color);
      padding: 2rem;
      border-radius: 2rem;
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .value {
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: bold;
      color: var(--text-color);
      margin-bottom: 0.5rem;
    }

    .label {
      color: var(--text-color-light);
      font-size: clamp(1rem, 2vw, 2rem);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .footer {
      background: var(--cards-background-color);
      padding: 1rem 2rem;
      text-align: center;
      color: var(--text-color-light);
      border-top: 1px solid var(--border-color);
      font-size: 1rem;
    }

    /* Modern responsive design using CSS Grid and Flexbox */
    /* All responsive behavior is now handled by clamp(), min(), and auto-fit */

    /* Mobile-first responsive table */
    @media (max-width: 768px) {
      body {
        background-color: red;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Skunk Report</h1>
      <p>Code Quality vs. Code Coverage Analysis</p>
    </div>

    <section class="cards">
      <div class="card">
        <div class="value">1</div>
        <div class="label">Modules Analysed</div>
      </div>
      <div class="card">
        <div class="value">833.04</div>
        <div class="label">Total Skunk Score</div>
      </div>
      <div class="card">
        <div class="value">833.04</div>
        <div class="label">Average Skunk Score</div>
      </div>
    </section>

    <section>
      <h2>Skunk Analysis</h2>
      <table>
      </table>
    </section>

    <footer class="footer">
      <p>Generated with Skunk v<%= data.skunk_version %> on <%= data.generated_at %></p>
    </footer>
  </div>
</body>
</html>
